name: jest
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v2
      
      - name: Install Modules
        run: yarn install

      - name: Run Unit Tests
        run: yarn test:unit
